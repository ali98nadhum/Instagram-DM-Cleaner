{
  "name": "instagram-message-cleaner",
  "version": "1.0.0",
  "description": "Bulk-unsend your own Instagram DM messages, as a Tampermonkey userscript. No backend, no build step at runtime.",
  "main": "src/content.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.scripts.json && node scripts/build-userscript.js",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
